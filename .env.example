# quran-khatmah backend-only service
PORT=3000
# Comma-separated allowlist of origins permitted to connect over Socket.IO.
# Leave unset to reflect the request origin (fine for local dev).
FRONTEND_URL=http://localhost:5173
